import 'package:flutter/material.dart';

class CheckIdPage extends StatelessWidget {
  const CheckIdPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}
